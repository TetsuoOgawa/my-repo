name: Github Output
on: push
jobs:
  share:
    runs-on: ubuntu-latest
    steps:
      - id: source
        run: echo "result=Hello" >> ${Github.Output}
      - env:
          RESULT: ${{ steps.source.outputs.result }}
        run: echo ${RESULT}
